---
lvm::volume_groups:
  datavg:
    physical_volumes:
      - /dev/sdb
    logical_volumes:
      data1:
        size: 10G
        mountpath: /mnt/data1

aptcacherng:
  cachedir: /mnt/data1/apt-cache